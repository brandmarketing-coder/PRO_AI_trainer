# 複製此檔為 .env 並填入 API 金鑰。可選 OpenAI 或 Anthropic（Claude）其中一家。

# ── 要用哪家？（openai 或 anthropic）──
# 不填會自動判斷：有 OPENAI_API_KEY 就用 openai，否則用 anthropic。
# 建議明確指定，切換才不會混淆：
PROVIDER=openai

# ── OpenAI（到 https://platform.openai.com 取得金鑰）──
OPENAI_API_KEY=sk-xxxxxxxx
# 選填：模型（預設 gpt-4o）；可改 gpt-4o-mini（更快更省）、gpt-4.1、gpt-4.1-mini
# OPENAI_MODEL=gpt-4o

# ── Anthropic / Claude（到 https://platform.claude.com 取得金鑰）──
# 要切回 Claude：把 PROVIDER 改成 anthropic，並填下面兩行
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
# MODEL=claude-sonnet-4-6

# ── 選填：伺服器埠號（預設 3000）──
# PORT=3000

# ── 後台密碼（兩級權限）──
# 主管密碼：只能看「分數彙整」（預設 12890464）
# REPORT_PASSWORD=12890464
# 管理員密碼：知識庫管理、系統管理（名單/開關/公告/日誌）、資料備份
# ⚠️ 未設定 ADMIN_PASSWORD 時，主管密碼會直接擁有完整管理權限（舊版相容）；正式交接前務必設定
# ADMIN_PASSWORD=請自訂一組不同於主管密碼的字串

# ── 選填：演練紀錄轉送到 n8n（供整理進 Google Sheet）──
# 在 n8n 建 Webhook 節點後，把 Production URL 貼在這裡
# N8N_WEBHOOK_URL=https://your-n8n-host/webhook/oright-training-record

# ── 選填：知識庫管理後台上傳的檔案存回 GitHub ──
# 需要一組具 repo 內容寫入權限的 GitHub Token（Fine-grained：Contents = Read and write）
# 未設定時，上傳只會寫入伺服器本機（Render 重新部署後會消失），故正式環境務必設定。
# GITHUB_TOKEN=github_pat_xxxxxxxx
# GITHUB_REPO=brandmarketing-coder/PRO_AI_trainer
# GITHUB_BRANCH=main
# GITHUB_KNOWLEDGE_DIR=knowledge
